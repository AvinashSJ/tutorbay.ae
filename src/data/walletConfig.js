// Wallet configuration — single source of truth for all credit/coin values
// Change these values to update credits across the entire app

export const WALLET_CONFIG = {
  UNLOCK_REQUIREMENT_COST: 4,
  UNLOCK_TUTOR_CONTACT_COST: 4,
  TUTOR_WELCOME_CREDITS: 15,
};